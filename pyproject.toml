[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ontheblock-ai-chatbot-service"
version = "0.1.0"
description = "ONTHEBLOCK AI chatbot gRPC service skeleton."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "asyncpg>=0.29,<1",
    "grpcio>=1.62,<2",
    "grpcio-health-checking>=1.62,<2",
    "protobuf>=4.25,<6",
    "redis>=5,<6",
]

[project.optional-dependencies]
dev = [
    "grpcio-tools>=1.62,<2",
    "pytest>=8,<9",
    "ruff>=0.5,<1",
]

[project.scripts]
chatbot-service = "chatbot_service.main:main"
chatbot-migrate = "chatbot_service.storage.migrations:main"
chatbot-validate = "chatbot_service.validation.cli:main"
chatbot-gcp-staging-check = "chatbot_service.deployment.gcp_staging_check:main"
chatbot-gcp-staging-acceptance = "chatbot_service.deployment.gcp_staging_acceptance:main"
chatbot-gcp-staging-deploy = "chatbot_service.deployment.gcp_staging_deploy:main"
chatbot-gcp-staging-readiness = "chatbot_service.deployment.gcp_staging_readiness:main"
chatbot-gcp-staging-secrets = "chatbot_service.deployment.gcp_staging_secrets:main"
chatbot-gcp-staging-validate = "chatbot_service.deployment.gcp_staging_validate:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["src/chatbot_service/generated"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
